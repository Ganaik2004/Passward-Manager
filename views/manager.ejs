<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <!-- GOOGLE FONT LINK -->
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
     <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap"
         rel="stylesheet">
         <!-- Googlr icon link -->
         <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
     <!-- Box icon link -->
     <link rel="stylesheet" href="https://unpkg.com/boxicons@latest/css/boxicons.min.css">
     <!-- FONT AUSOME LINK -->
     <script src="https://kit.fontawesome.com/659a986349.js" crossorigin="anonymous"></script>
     <style>
        * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Poppins', sans-serif;
    text-decoration: none;
    list-style: none;
    scroll-behavior: smooth;
}

:root {
    --bg-color: #080808;
    --bg-2: #262626;
    --color: #fff;
    --main-color: #24cbd7   ;
    --color-2: #c8c8c8;
    --h1-font: 2.5rem;
    --p-font: .9rem;
}
body {
    background-color: var(--bg-color);
    color: var(--color);
    overflow-x: hidden;
}
body.dark {
    --bg-color: #fff;
    --color: #080808;
    --main-color: rgb(36, 203, 215);
    --color-2: #616161;
    --bg-2: #f6f6f6;
}
.container{
    max-width: 80vw;
    margin-inline: auto;
    padding-inline: 20px;
    margin-top: 20px;
    text-align: center;
  display: flex;
  flex-direction: column;
  gap: 20px;
  align-items: center;
  justify-content: center;
}
nav{
    padding-block: 15px;
}
 ul{
    display:flex;
    justify-content: center;
    gap: 60px;
 }
 ul li{
    cursor: pointer;
 }
 table{
    border: 1.7px solid var(--color);
 }
 th{
    color: var(--main-color);
    font-size: 1.3rem;
    padding: 10px;
 }
 td{
    padding: 10px;
 }
 h1,h2,h3{
    margin: 23px 0;
 }
 button{
    display: block;
    /* margin-block: 20px; */
    padding: 9px 18px;
    font-size: 1.1rem;
    background-color: transparent;
    border: 2px solid var(--main-color);
    font-weight: 600;
    color: var(--main-color);
    border-radius: 10px;
    transition:all .5s ease;
    display: flex;
    align-items: center;
    gap: 15px;
    cursor: pointer;
 }
 button i{
    font-size: 22px;
    animation:  float ease 2s infinite;
 }
 @keyframes float{
    0%{
        transform: translateX(0px);
    }
    50%{
        transform: translateX(-5px);
    }
    100%{
        transform: translateX(0px);
    }
 }
 button:hover{
    background-color: var(--main-color);
    color: var(--bg-color);
 }
 
     </style>
</head>
<body>
    <nav>
        <ul>
            <li>HOME</li>
            <li>ABOUT</li>
            <li>CONTACT</li>
        </ul>
    </nav>
    <div class="container">
        <h1>Password Manager</h1>
        <p>Discover the future of password management! Our user-friendly, secure, and feature-rich password manager simplifies digital life. Safeguard your accounts effortlessly</p>
        <h2>Your Passwords</h2>
        <table>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Email</th>
                <th>Password</th>
            </tr>
            <%for(user of user){%>
                <tr>
                    <td><%=user.id%></td>
                    <td><%=user.username%></td>
                    <td><%=user.email%></td>
                    <td><%=user.password%></td>
                    <td>
                    <form method="GET" action="/manager/<%=user.id%>/edit">
                       <button>Edit</button>
                    </form>
                </td>
                <td>
                    <form method="GET" action="/manager/<%=user.id%>/delete">
                       <button>Delete</button>
                    </form>
                </td>
                </tr>
           <% }%>
        </table>
        <form action="/manager/add">
            <button>Add Password</button>
        </form>
        <!-- <h3>Add a Password</h3>
        <form action="/submit" method="post">
            <input type="text" name="username" id="username" placeholder="Username" required>
            <input type="password" id="password" name="password" required placeholder="Enter the Password">
            <button type="submit">Submit <i class="fa-solid fa-arrow-right"></i></button>
        </form> -->
    </div>
   
    <script src="script.js"></script>
</body>
</html>